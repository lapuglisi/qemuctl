***** qemuctl/runtime
** validate machine status through pid file
** add statuses running && started
--- when running with no daemonize:
+++ add signal handling to update machine status OR
+++ use process.wait to check for process exit signal
